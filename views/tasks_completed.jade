extends layout

block content

	.list
		if(tasks.length === 0 )
			| No hay tareas completadas
		each task, index in tasks
			.item
				div.num
					span=index+1
						|.&nbsp;
				div.name.completed-task
					span.name=task.name